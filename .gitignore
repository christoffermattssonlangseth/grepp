# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/
.DS_Store

# Keep the LiftLog source files (they live in ios/LiftLog/).
# The generated .xcodeproj can be committed or ignored — your call:
# *.xcodeproj/

# Swift Package Manager
.build/
# Xcode generates this when you open Package.swift — not source.
.swiftpm/

# Secrets — this repo is public. Never commit an API key.
# The Coach tab reads the Claude key from the Keychain or the ANTHROPIC_API_KEY
# scheme variable. A Secrets.plist is ignored here and excluded from the build.
Secrets.plist
LiftLog/Secrets.plist
.env
*.local.xcconfig
